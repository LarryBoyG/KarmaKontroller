# Optional proxy upstream for Karma Mapbox repair.
#
# Use the public proxy to avoid needing the Windows PC agent on the same network:
#
karmakontroller.duckdns.org:443
#
# To fall back to a same-network Windows PC agent, replace the line above with
# your PC LAN address, for example:
#
# 192.168.1.50:443
